<div><img src="{{image_url}}" alt="{{id}} image"></div>
<div>
    <h1>{{name}}</h1>
    <h2>{{tagline}}</h2>
    <p>{{description}}</p>
    <h3>Food Pairings</h3>
    <ul>
        {{#each food_pairing}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
    <p><b>Brewers Tips: </b> {{brewers_tips}}</p>
</div>